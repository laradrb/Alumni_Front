import React from 'react';
import { CardContainer, CardTitle, Text, Button } from './styleCard'; // Asegúrate de que estos estilos estén en un archivo 'StyledComponents.jsx'

const CardCoder = (title, text, button) => {
  return (
    <CardContainer>
      <CardTitle>Coders</CardTitle>
      <Text>Acceso a Oportunidades de Empleo
Comunidad y Colaboración<br/>
Visibilidad ante Empresas</Text>
      <Button to="/registroCoder">Registrar como Alumni</Button>
    </CardContainer>
  );
};

export default Card;
